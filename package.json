{
    "name": "times",
    "version": "0.0.1",
    "private": true,
    "engines": {
        "node": "6.9.0",
        "npm": "4.1.1"
    },
    "scripts": {
        "postinstall": "cd client && yarn install",
        "dev": "echo 'visit http://localhost:3000' && foreman start -f Procfile.dev",
        "test": "rspec"
    },
    "dependencies": {
        "moment": "^2.18.1",
        "react-redux": "^5.0.3",
        "redux": "^3.6.0"
    }
}
