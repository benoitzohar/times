{
    "name": "times",
    "version": "0.0.1",
    "private": true,
    "engines": {
        "node": "6.9.0",
        "npm": "4.1.1"
    },
    "scripts": {
        "postinstall": "cd client && yarn install",
        "heroku-prebuild": "yarn postinstall",
        "dev": "echo 'visit http://localhost:3000' && foreman start -f Procfile.dev",
        "test": "rspec"
    },
    "dependencies": {
        "create-react-native-app": "^0.0.3",
        "global": "^4.3.1"
    }
}
